{
	"name": "prehevil-treaty-eta-backend",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "tsx watch src/index.ts",
		"start": "prisma migrate deploy && node src/index.ts",
		"prisma:generate": "prisma generate"
	},
	"dependencies": {
		"@prisma/client": "^6.15.0",
		"h3": "^2.0.0-beta.0"
	},
	"devDependencies": {
		"@types/node": "^24.0.1",
		"prisma": "^6.15.0",
		"tsx": "^4.20.3"
	}
}
